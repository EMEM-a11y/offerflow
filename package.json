{
  "name": "offerflow-job-assistant",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "packageManager": "pnpm@10.13.1",
  "engines": { "node": ">=22.12.0" },
  "scripts": {
    "dev": "vite --host 127.0.0.1 --port 5174 --strictPort",
    "build": "vite build",
    "test": "node --test tests/*.test.mjs",
    "check:release": "node scripts/check-release.mjs",
    "update:jobs": "node scripts/update-jobs.mjs",
    "build:bank": "node scripts/build-public-question-bank.mjs",
    "build:graphics": "node scripts/build-graphic-question-bank.mjs",
    "build:charts": "node scripts/build-data-chart-bank.mjs",
    "check:duplicates": "node scripts/check-question-duplicates.mjs",
    "preview": "vite preview --host 0.0.0.0"
  },
  "devDependencies": {
    "vite": "^7.1.4"
  },
  "dependencies": {
    "@supabase/supabase-js": "^2.115.0"
  }
}
